{
  "name": "shell",
  "description": "Connect to the server with a shell interface rendered as a virtual terminal in the world.",
  "version": "0.0.1",
  "client": "client.js",
  "server": "server.js",
  "scripts": {
    "postinstall": "scripts/postinstall.sh"
  },
  "dependencies": {
    "get-random-port": "0.0.1",
    "http-proxy": "^1.15.2",
    "socket.io": "^1.7.0",
    "socket.io-client": "^1.7.0"
  }
}
